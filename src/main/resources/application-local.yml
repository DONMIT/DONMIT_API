spring:
  datasource:
    url: jdbc:mysql://localhost:3306/donmit
    username: root
    password: donmit

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect